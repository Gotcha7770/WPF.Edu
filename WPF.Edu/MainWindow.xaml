<Window x:Class="WPF.Ext.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Ext"
        mc:Ignorable="d"
        Title="MainWindow"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.CommandBindings>
        <CommandBinding Command="Delete" Executed="CommandBinding_Executed"/>
    </Window.CommandBindings>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ListBox Name="Views" 
                 Width="300"
                 ItemsSource="{Binding ViewsCollection}"
                 ItemTemplate="{StaticResource ViewItemTemplate}"/>

        <ContentControl Grid.Column="1" Content="{Binding ElementName=Views, Path=SelectedValue}"/>
    </Grid>
    
</Window>
