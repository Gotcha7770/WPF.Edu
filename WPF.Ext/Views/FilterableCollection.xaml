<UserControl x:Class="WPF.Ext.Views.FilterableCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:me="clr-namespace:WPF.Ext.MarkupExtensions"
             xmlns:local="clr-namespace:WPF.Ext.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <UserControl.Resources>
        <ResourceDictionary>

            <DataTemplate x:Key="ItemTemplate">
                <TextBlock Text="{Binding}"/>
            </DataTemplate>

            <CollectionViewSource x:Key="ItemsSource" Source="{Binding Items}">
                <CollectionViewSource.Filter>
                    <me:Filter>
                        <me:PropertyFilter PropertyName="Value" Value="Text1" />
                    </me:Filter>
                </CollectionViewSource.Filter>
            </CollectionViewSource>

        </ResourceDictionary>
    </UserControl.Resources>

    <ListView ItemsSource="{Binding Source={StaticResource ItemsSource}}"
              ItemTemplate="{StaticResource ItemTemplate}"/>
</UserControl>
